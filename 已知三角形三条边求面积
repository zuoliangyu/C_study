#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<math.h>
int main()
{
	float a, b, c, p,S;
	printf("please input a,b,c:");
	scanf("%f%f%f", &a, &b, &c);
	p = (a + b + c) / 2;
	S = sqrt(p * (p - a) * (p - b) * (p - c));
	printf("%f", S);
	return 0;
}
